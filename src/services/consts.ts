import path from "node:path";

export const LAB_PATH = path.join(
  process.cwd(),
  "src",
  "app",
  "labs",
  "(content)",
);
export const PROJECT_PATH = path.join(
  process.cwd(),
  "src",
  "app",
  "projects",
  "content",
);
